class ProDetails {
    constructor(
        proIds,
        estimateDuration,
        needsTools,
        needsPicture,
        needsDimensions,
    ) {
        this.proIds = proIds,
        this.estimateDuration = estimateDuration,
        this.needsTools = needsTools,
        this.needsPicture = needsPicture,
        this.needsDimensions = needsDimensions
    }
}

export default ProDetails;